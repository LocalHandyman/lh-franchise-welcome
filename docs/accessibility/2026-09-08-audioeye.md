# AudioEye remediation — September 8, 2026

Scope: Welcome 088–123. All changes require AudioEye retest before findings are closed.

FAQ uses h3/button, expanded state, controlled hidden answers and ul/li. Added main and named form landmarks; navigation and references are lists; citations sit inside the page footer with heading hierarchy. Split visual headings expose one continuous text equivalent. Hash navigation moves keyboard focus to the target heading or application input. Phone/email links have contextual names.

Application inputs have explicit visible labels, names/autocomplete and error associations. Invalid submission announces all invalid fields in a focused error summary with field links; prevents external submission; server failure remains an error instead of displaying success. Native success dialog supplies modal semantics. Existing endpoint and lead payload retained.

Existing native videos now have contextual accessible names and explicit keyboard focus. The investment-thesis fake player was only a styled div, with no media source or click handler; replaced with explanatory text. No video content was deleted because none was wired there. Welcome 120/121 are addressed by removing the nonfunctional control; restoring a real thesis video requires a supplied media asset.

Validation: component checks passed for main/form landmarks, six missing fields, error focus/link, no request on invalid submission, FAQ semantics/state, skip focus, footer citations, named keyboard-focusable videos. Build/browser evidence is recorded in the PR and ADA sheet. No real lead or email was sent during tests.

Welcome 124/125 (blank arrow-navigation stops around video wrappers) need AudioEye/NVDA reproduction. Wrappers have no role or tabindex; hiding entire wrappers would hide the real videos. These are not marked fixed or verified.
